# Copy to .env locally — never commit real secrets.

# PostgreSQL (API)
ConnectionStrings__Default=Host=localhost;Port=5432;Database=modelaflow_dev;Username=modelaflow;Password=CHANGE_ME

# Next.js → API (apps/web)
# Must match apps/api launchSettings (http profile uses port 5074).
NEXT_PUBLIC_API_URL=http://localhost:5074

# Legacy alias (prefer NEXT_PUBLIC_API_URL)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5074
